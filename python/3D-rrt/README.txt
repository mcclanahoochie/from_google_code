To run a sample visualization, run the "planner_example.py" in the rrt-planner folder

To run the same script used to bench the two algorithms in the paper, run the "bench_planner_example.py" in the rrt-planner folder.

example: 
  $ python planner_example.py 
